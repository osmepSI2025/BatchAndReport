@{
    ViewData["Title"] = "เรียกข้อมูลพนักงาน";
}

<h2>เรียกข้อมูลพนักงานจาก API</h2>

<button id="btnCallApi" class="btn btn-primary">เรียก API</button>

<div id="result" style="margin-top: 20px; white-space: pre-wrap; font-family: monospace;"></div>

<h2>เรียกข้อมูลพนักงานที่มีการโยกย้ายจาก API</h2>

<button id="btnCallApiMovement" class="btn btn-primary">เรียก API</button>

<div id="resultMovement" style="margin-top: 20px; white-space: pre-wrap; font-family: monospace;"></div>

<h2>เรียกข้อมูลตำแหน่งจาก API</h2>

<button id="btnCallApiPosition" class="btn btn-primary">เรียก API</button>

<div id="resultPosition" style="margin-top: 20px; white-space: pre-wrap; font-family: monospace;"></div>

<h2>เรียกข้อมูล business-units จาก API</h2>

<button id="btnCallApiBusinessUnit" class="btn btn-primary">เรียก API</button>

<div id="resultBusinessUnit" style="margin-top: 20px; white-space: pre-wrap; font-family: monospace;"></div>

@section Scripts {
    <script>
        document.getElementById("btnCallApi").addEventListener("click", async function () {
            const page = 1;
            const perPage = 10;

            const url = `/api/Job/GetEmpHR?page=${page}&perPage=${perPage}`;

            try {
                const response = await fetch(url, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    }
                });

                if (!response.ok) {
                    throw new Error("เกิดข้อผิดพลาด: " + response.status);
                }

                const data = await response.json();
                document.getElementById("result").innerText = JSON.stringify(data, null, 2);
            } catch (error) {
                document.getElementById("result").innerText = "❌ ERROR: " + error.message;
            }
        });
    </script>
    <script>
        document.getElementById("btnCallApiMovement").addEventListener("click", async function () {
            const page = 1;
            const perPage = 10;

            const url = `/api/Job/GetEmpMovement?page=${page}&perPage=${perPage}`;

            try {
                const response = await fetch(url, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    }
                });

                if (!response.ok) {
                    throw new Error("เกิดข้อผิดพลาด: " + response.status);
                }

                const data = await response.json();
                document.getElementById("resultMovement").innerText = JSON.stringify(data, null, 2);
            } catch (error) {
                document.getElementById("resultMovement").innerText = "❌ ERROR: " + error.message;
            }
        });
    </script>
    <script>
        document.getElementById("btnCallApiPosition").addEventListener("click", async function () {
            const page = 1;
            const perPage = 10;

            const url = `/api/Job/GetPosition?page=${page}&perPage=${perPage}`;

            try {
                const response = await fetch(url, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    }
                });

                if (!response.ok) {
                    throw new Error("เกิดข้อผิดพลาด: " + response.status);
                }

                const data = await response.json();
                document.getElementById("resultPosition").innerText = JSON.stringify(data, null, 2);
            } catch (error) {
                document.getElementById("resultPosition").innerText = "❌ ERROR: " + error.message;
            }
        });
    </script>
    <script>
        document.getElementById("btnCallApiBusinessUnit").addEventListener("click", async function () {
            const page = 1;
            const perPage = 10;

            const url = `/api/Job/GetBusinessUnit?page=${page}&perPage=${perPage}`;

            try {
                const response = await fetch(url, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    }
                });

                if (!response.ok) {
                    throw new Error("เกิดข้อผิดพลาด: " + response.status);
                }

                const data = await response.json();
                document.getElementById("resultBusinessUnit").innerText = JSON.stringify(data, null, 2);
            } catch (error) {
                document.getElementById("resultBusinessUnit").innerText = "❌ ERROR: " + error.message;
            }
        });
    </script>
}
